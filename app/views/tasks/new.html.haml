.row
  .col-md-3
    - if policy(course).list_tasks?
      = btn_to course_tasks_path(course) do
        = icon 'arrow-left', t(:back)

  .col-md-9
    %h1= course.name
    %h2= t '.title'
    = render 'form'
