- content_for :sidebar do
  - if policy(task).destroy?
    .delete= link_to t('.delete'), [course, task], method: :delete

- breadcrumb task

.page-header
  %h1= course.name

%h2= t '.title'

= render 'form'
