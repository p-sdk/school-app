.row
  .col-md-3
    - if policy(course).show?
      = link_to course, class: 'btn btn-default' do
        = icon 'arrow-left', 'Wróć'

  .col-md-9
    %h1= course.name
    %h2 Zapisani studenci
    %ul.students= render course.students
