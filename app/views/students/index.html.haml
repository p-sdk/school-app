.row
  .col-md-3
    - if policy(course).show?
      = btn_to course do
        = icon 'arrow-left', 'Wróć'

  .col-md-9
    %h1= course.name
    %h2 Zapisani studenci
    %ul.students= render students
