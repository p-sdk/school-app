.row
  .col-md-3
    - if policy(course).list_lectures?
      .back
        = btn_to course_lectures_path(course) do
          = icon 'arrow-left', 'Wróć do listy wykładów'

    - if policy(lecture).edit?
      .edit
        = btn_to edit_course_lecture_path(course, lecture) do
          = icon 'edit', 'Edytuj'

  .col-md-9
    %h1= course.name
    %h2= lecture.title
    .lecture= lecture.content_formatted
    - if lecture.attachment?
      = link_to lecture.attachment.url, target: :_blank do
        = icon 'paperclip', lecture.attachment_file_name
        = lecture.attachment_file_name_formatted
