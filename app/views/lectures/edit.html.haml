- content_for :sidebar do
  - if policy(lecture).show?
    .back= link_to t(:back), [course, lecture]

  - if policy(lecture).destroy?
    .delete= link_to t('.delete'), course_lecture_path(course, lecture), method: :delete

%h1= course.name
%h2= t '.title'
= render 'form'
