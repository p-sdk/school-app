.row
  .col-md-3
    - if policy(category).show?
      = btn_to category do
        = icon 'arrow-left', 'Wróć'

  .col-md-9
    %h1 Edytuj kategorię
    = simple_form_for category do |f|
      = render 'fields', f: f
      = f.button :submit, 'Zapisz zmiany'
