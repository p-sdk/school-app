- content_for :sidebar do
  - if policy(Category).index?
    .back= link_to t('.back_to_categories'), categories_path

%h1= category.name

- if policy(category).edit?
  .edit= link_to t(:edit), edit_category_path(category)

- if policy(category).destroy?
  .delete= link_to t(:delete), category_path(category), method: :delete

%ul= render category.courses
